---
import EditorialPageLayout from '../layouts/EditorialPageLayout.astro';
import { getEditorialPage } from '../lib/content/pages';

const content = await getEditorialPage('process');
if (!content) throw new Error('Process page content is unavailable.');
---

<EditorialPageLayout {content} />
